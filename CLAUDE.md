# Desktop Monster

Read `AGENTS.md` first — it is the single source of truth for build/test/run/package
commands and for what must pass before anything is declared done.

Harness operators: see `.harness/CURRENT` for the active harness version and
`.harness/<version>/HARNESS.md` for how to run the autonomous loop.
