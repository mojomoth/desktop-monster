// Preload stub — the contextBridge `window.desmon` API lands in T03.
// Must stay CJS-compatible (sandboxed preload); emitted as CommonJS by
// tsconfig.main.json (module node16, no "type":"module" in package.json).

export {};
